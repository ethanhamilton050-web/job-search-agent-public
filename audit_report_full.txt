huntCount=44 confirmedCount=42
[fix-attempted] sev=high votes=3/3real browser=False -- dashboard.py:351
  SUMMARY: The /answers POST handler rebuilds the entire screening_answers.json store from only the fields present in the submitted form and calls qbank.save() (a full overwrite, not a merge), so any answer parked by a concurrent process after the page was loaded is silently deleted.
  SCENARIO: Reproduced directly against jobagent/qbank.py: load a store with two existing Q&As (simulating what the GET /answers page renders as hidden inputs), then call qbank.record_unknown() for a brand-new question the way the Workday filler does mid-run (this is exactly what happens when you click 'Run queue' or 'Apply' and the autofiller hits an unanswerable screening question while you already have the Answers tab open). Then submit the browser form exactly as dashboard.py's handler does (qs=request.form.getlist('q'), ans=request.form.getlist('a'), data=dict(zip(...)), qbank.save(data)). Result: the on-disk store goes from 3 questions (2 answered + 1 freshly parked) down to 2 - the newly parked question the running/queued applier needs answered is gone. The same clobber happens with nothing exotic at all: just open the Answers page in two browser tabs (or leave it open across a page refresh triggered by anything else) and Save from the stale one - whichever Save lands last wins and erases the other tab's edits and any answers parked in between. There is no merge-by-question-text, no last-modified check, and no warning to the user that data was lost.
  AGENT_FIX_REPORT: None

[flagged-not-autofixed] sev=high votes=3/3real browser=False -- jobagent/applyqueue.py:112
  SUMMARY: next_queued() (read) and mark(..., 'running') (write) in run()'s claim step are two separate unguarded statements, so two concurrently-running queue processes can both pick up and process the same listing_id.
  SCENARIO: dashboard.py's /queue/run guard (dashboard.py:449-456) reads applyqueue.pending()/next_queued() to decide whether it's safe to launch a new `main.py queue run` subprocess, but subprocess.Popen returns immediately without waiting for the child to actually mark anything 'running' - two rapid POSTs (double-click, retried request) can both see 'not already_running' and both launch a grinder. Both processes then call applyqueue.run(): both call next_queued(conn) (applyqueue.py:112) and get the SAME earliest queued listing_id before either has executed mark(conn, lid, 'running') (line 115). Both then call apply_fn(lid) - i.e. two independent Workday/browser automations fire concurrently against the same job application, each also independently consuming an attempts-cap slot for that company. This also defeats the documented anti-ban pacing ('Pacing ... is applied only BETWEEN jobs - human-like, and anti-ban', applyqueue.py:106-107): the random 90-240s gap is only enforced within a single process's loop, so two concurrent processes can fire two automated applications milliseconds apart.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=3/3real browser=False -- jobagent/applyqueue.py:33
  SUMMARY: enqueue() unconditionally resets any existing row to 'queued' (INSERT OR REPLACE) with no guard for state=='running', and the UI offers no protection against re-queuing a listing that is currently being processed.
  SCENARIO: The dashboard's '+queue' button (dashboard.py:150, POST /queue/add/<lid> at dashboard.py:430-438) is shown on every listing row unconditionally - it does not check or disable itself for a listing_id whose apply_queue row is currently 'running'. If a user clicks '+queue' on a job that a `queue run` batch is actively processing, applyqueue.enqueue() (applyqueue.py:36-40) does INSERT OR REPLACE, resetting state to 'queued'/detail to NULL/updated_at to now - discarding the 'running' marker. When the in-flight run() loop's apply_fn(lid) call finishes, run()'s terminal mark(conn, lid, final_state, detail) (applyqueue.py:119 or 121) unconditionally overwrites the row again, stamping the STALE run's outcome (e.g. 'filled') over the fresh 'queued' state the user just set - silently discarding the intended re-run/retry with no error or warning.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=3/3real browser=False -- jobagent/attempts.py:45
  SUMMARY: Per-company attempt cap is check-then-act across two separate DB statements with no transaction/lock, so concurrent processes can each read a stale count and both pass the check.
  SCENARIO: attempts.allowed() (SELECT count) and attempts.record() (UPSERT count+1) are separate round-trips (attempts.py:45-50 and 53-66), called back-to-back but non-atomically at main.py:413/418 (cmd_apply) and main.py:487/496 (cmd_queue's apply_one). Nothing prevents two OS processes from racing: e.g. a user clicks 'Apply' on two different Citi listings on the dashboard within the same second (dashboard.py:394, subprocess.Popen per click, no cross-request coordination). Both spawned `main.py apply` processes take ~1s to start up and connect to the DB - each reads count=2 (CAP-1), both pass `allowed`, both call `record`, and the count becomes 4. The cap (CAP=3) is silently exceeded, defeating the exact anti-flagging safety rail the module exists for. Same race applies between a manual `apply` run and a concurrently-running `queue run` batch hitting the same company.
  AGENT_FIX_REPORT: None

[flagged-not-autofixed] sev=high votes=3/3real browser=False -- jobagent/guardrail.py:79
  SUMMARY: verify_ai_answer (Layer 2) never checks that the cited field is topically relevant to the question — it only checks the field exists and its boolean value happens to match the AI's claimed Yes/No.
  SCENARIO: Confirmed by running the real code: answers={'work_authorized': True,'needs_sponsorship': False,'is_over_18': True,'is_veteran': False}, question='Have you ever been terminated from a previous job for cause, or convicted of a felony?', ai_answer_fn=lambda q: ('No','is_veteran'). screening.answer_questions(...) returns {'answer': 'No', 'source': 'is_veteran', 'layer': 'ai+receipt'} — i.e. it is surfaced as a verified, trustworthy answer to a felony/termination-for-cause disclosure question, purely because is_veteran happens to also be False. Any AI (fabricating, hallucinating, or just prompt-confused) can pass Layer 2 for ANY question by citing whichever real answer-bank field currently holds the boolean value it wants to assert — the 'receipt' proves the field is real and its value matches, never that the field answers THIS question. The test suite already documents this gap as accepted behavior: tests/test_screening.py:81-88 (test_ai_survives_only_with_a_real_receipt) accepts 'work_authorized' as the cited receipt for a completely unrelated 'Do you consent to a background check?' question.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=3/3real browser=False -- jobagent/guardrail.py:55
  SUMMARY: The Layer 1 _RULES regex table uses unanchored substring / greedy `.*` patterns (e.g. bare 'sponsor', bare '\breserves\b', 'authoriz.*work' with no distance limit) that fire on topically unrelated questions, which are then auto-answered with full confidence from the wrong field — Layer 1 has NO receipt check at all once a field is non-empty.
  SCENARIO: Confirmed by running guardrail.resolve() directly against plausible real screening questions with FULL={'work_authorized': True,'needs_sponsorship': False,'is_over_18': True,'is_veteran': False}: (1) 'Do you authorize Citi to conduct a background check of your prior work history and criminal record?' -> resolves via work_authorized (background-check consent, not work-authorization). (2) 'Will you be available to start work on 18 June if an offer is extended?' and 'Have you worked here for at least 18 months...' -> resolve via is_over_18 (a start-date/tenure question, not age). (3) 'Are you a sponsor or organizer of any outside charitable events using company resources?' -> resolves via needs_sponsorship (unrelated topic). (4) Most seriously: 'Have you ever been convicted of a felony or misdemeanor involving military-grade weapons or explosives?' and 'Do you currently maintain any brokerage, deposit, or reserves accounts with Citi or its affiliates that would need to be disclosed?' both silently resolve to 'No' via the is_veteran field — a criminal-history/financial-disclosure question answered from a field about military service, with zero human review, because Layer 1 output is trusted unconditionally whenever the field is populated.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=3/3real browser=False -- jobagent/matchcoach.py:61
  SUMMARY: local_rewrite() does a raw json.loads() on the model's chat reply with no tolerance for markdown fences or extra text, unlike this same codebase's fieldmap.py which already had to solve this exact problem.
  SCENARIO: Ollama/gemma models very commonly wrap a requested JSON reply in ```json ... ``` fences or add a sentence of preamble, even when told 'Reply with ONLY JSON'. When that happens here, `json.loads(content)` raises ValueError, which is caught by the blanket `except (..., ValueError, ...)` and local_rewrite() silently returns None -- for every single bullet, every single time. suggest_all() then returns an empty list and dashboard.py's /coach/<lid> page shows 'No honest rewrite found ... or the local AI box (Ollama) isn't running', which is actively misleading: Ollama is running fine, the parsing is just brittle. Contrast with jobagent/fieldmap.py's parse_map() (lines 124-129), which already handles this in the same codebase by extracting the substring between the first '[' and last ']' instead of assuming the whole reply is pure JSON -- matchcoach.py doesn't reuse that lesson.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=2/2real browser=False -- jobagent/qbank.py:39
  SUMMARY: answer()/pending() return on the FIRST loosely-matching key, so a duplicate entry for the 'same' question (different case/punctuation) can permanently shadow the human's real answer -- no concurrency even required.
  SCENARIO: Reproduced directly: (1) a worker calls record_unknown('Are you currently employed by PNC?') -> store becomes {'Are you currently employed by PNC?': ''}. (2) The human uses the dashboard's own 'Add a question you know you'll be asked' box (dashboard.py /answers POST, lines 351-360) and types it slightly differently -- 'Are You Currently Employed By PNC' (no '?') -- with answer 'Yes'. Nothing in the POST handler or in qbank.save() runs the _norm()-based loose-dedup check that record_unknown() has (that check lives ONLY in record_unknown, lines 48-58); the new key is simply added: store becomes {'Are you currently employed by PNC?': '', 'Are You Currently Employed By PNC': 'Yes'}. (3) qbank.answer('are you currently employed by pnc') iterates dict items in insertion order and returns on the first _norm() match -- the ORIGINAL blank entry -- so it returns '' even though the human clearly answered 'Yes' moments ago. (4) qbank.pending() still lists the original wording as pending forever. Verified end-to-end with a real interpreter run: answer() returned '' and pending() still showed the question, even after the 'Yes' was saved. This directly undermines the store's stated invariant ('only a non-empty stored value is ever auto-filled') -- the filler will keep leaving the field blank/flagged even though the human answered it, and the Answers page will keep nagging for an answer that was already given under a slightly different spelling.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=3/3real browser=False -- jobagent/qbank.py:35
  SUMMARY: save()/record_unknown() do an unguarded read-modify-write with no file lock and no atomic replace; dashboard.py's /answers POST compounds this into a deterministic overwrite that silently deletes questions parked by a concurrently-running apply worker.
  SCENARIO: Human clicks Apply on a job (dashboard.py /apply/<lid>, line ~394) which spawns a detached subprocess driving applier.py/filler.py that can stay open for review for minutes. The human separately opens /answers, which snapshots qbank.load() into hidden form fields at that moment (dashboard.py line 362-364). While the human is still looking at that page, the still-running worker hits a new screening question and calls qbank.record_unknown() (applier.py:149), which loads the file, adds the new blank question, and saves -- succeeding. The human then clicks 'Save answers': dashboard.py's POST handler (lines 353-360) builds `data` ONLY from the form fields it already had (the stale snapshot) and calls qbank.save(data), which unconditionally overwrites the whole file. The question the worker just parked seconds earlier is gone with no warning, no merge, and no trace -- the one human-review opportunity qbank exists to create is lost silently. The same read-modify-write gap also means two concurrent record_unknown() calls (e.g. two Apply runs on two different jobs at once, or a queued batch run overlapping a manual Apply) can race and one save() clobber the other's freshly-added question.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=3/3real browser=False -- jobagent/qbank.py:31
  SUMMARY: load() calls json.loads() with no exception handling; a malformed/truncated screening_answers.json crashes every caller, including the dashboard's GET /answers route.
  SCENARIO: Verified directly: json.loads() on a truncated file (e.g. '{"Are you 18?": "Yes", ' -- exactly what a process killed mid-write, an interrupted save() from finding above, or a OneDrive sync conflict copy could leave behind, since this whole repo tree is OneDrive-synced per the workspace CLAUDE.md) raises `json.decoder.JSONDecodeError: Expecting property name enclosed in double quotes`. dashboard.py's GET /answers (line 362: `data = qbank.load()`) has no try/except around this call, so the exception propagates straight out of the Flask view function, producing an uncaught 500 and making the entire Answers page inaccessible until the file is manually repaired or deleted. The same crash would hit a live apply run the moment applier.py calls qbank.record_unknown() or a filler calls qbank.answer(), since both go through load() with no guard either.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=2/2real browser=False -- jobagent/screening.py:48
  SUMMARY: For a KNOWN universal question whose backing field is missing, empty, or holds a value guardrail._field_answer can't parse, screening.answer_questions falls through to the AI+receipt path instead of forcing NEEDS_HUMAN outright — and that AI path (per the Layer-2 gap above) can be satisfied by citing any other unrelated field.
  SCENARIO: Confirmed by running the real code: answers has is_over_18 stored as the string 'No.' (trailing period — not in guardrail's closed token set, so guardrail.resolve returns NEEDS_HUMAN even though the true stored fact is 'No'). With ai_answer_fn=lambda q: ('Yes','work_authorized'), screening.answer_questions([Q_18], answers, ai_answer_fn=ai) returns {'answer': 'Yes', 'source': 'work_authorized', 'layer': 'ai+receipt'} for 'Are you at least 18 years of age?' — the OPPOSITE of the actual stored answer, laundered through a completely unrelated field and labeled as a verified receipt-backed answer. The same happens if is_over_18/is_veteran/etc. is simply absent from the answer bank: the code does not treat 'field matched by Layer 1 but unresolved' as a hard stop, it treats it identically to 'no profile rule matched at all' and hands the question to the AI.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=3/3real browser=False -- jobagent/sources/workday.py:54
  SUMMARY: keep += [...] list comprehension runs outside the try/except that guards the POST+JSON parse, so a non-dict entry in Workday's jobPostings array crashes fetch() uncaught.
  SCENARIO: Workday's paged jobs POST returns 200 with jobPostings containing a null placeholder for a posting that failed to serialize server-side (a known real pattern for partial/incomplete records), e.g. {"jobPostings": [null, {...}]}. The try/except at lines 45-51 only wraps the request+`.json()` call; the following `keep += [j for j in posts if location_ok(j.get(...)) ...]` at lines 54-55 is unprotected, so `j.get(...)` on the null entry raises AttributeError, which propagates out of fetch() entirely uncaught.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=3/3real browser=False -- jobagent/sources/workday.py:70
  SUMMARY: url=root + "/" + site + j.get("externalPath", "") is built outside the try/except in _detail(), so a null externalPath (key present, value None) raises an uncaught TypeError.
  SCENARIO: .get(key, default) only substitutes the default when the key is absent, not when its value is null. If a Workday listing entry has "externalPath": null (plausible for a draft/removed posting still surfaced in the search feed), the line at 64 inside the try/except also fails but is caught (falls to `except: pass`); however the return statement at lines 68-72 re-evaluates `j.get("externalPath", "")` -> None, and `root + "/" + site + None` raises TypeError: can only concatenate str (not "NoneType") to str, uncaught. This happens inside ThreadPoolExecutor.map (line 75), which is also not wrapped in try/except, crashing fetch().
  AGENT_FIX_REPORT: None

[flagged-not-autofixed] sev=high votes=3/3real browser=False -- jobagent/sources/workday.py:70
  SUMMARY: A Workday listing's stored URL silently degrades to the bare tenant careers-site root (no job-specific path) if the API's per-posting record is missing externalPath, and jobagent/linkcheck.py's Workday shape-check can't tell the difference.
  SCENARIO: `url=root + "/" + site + j.get("externalPath", "")` defaults to "" when a Workday jobPostings entry lacks externalPath (partial/odd API response), producing e.g. `https://citi.wd5.myworkdayjobs.com/en-US/citi_careers` with no job segment. jobagent/linkcheck.py imports sources/workday.py's own `_URL_RE` to validate Workday links (linkcheck.py:24,38), but that regex was written to recognize a *site* URL (tenant + one site slug, no `$` anchor, no required job path) -- it happily matches the bare root too, so `looks_like_application_url` reports this malformed URL as 'looks fine'. That's a real asymmetry: the greenhouse/lever/ashby patterns in linkcheck.py each require an *extra* path segment beyond the org slug (and have a passing test for the bare-domain-with-no-posting case, e.g. test_lever_bare_domain_with_no_posting_fails / test_greenhouse_marketing_site_fails), but there is no equivalent 'workday bare-site-URL fails' test, and none would pass today. Workday is also the one ATS main.py's unattended `queue run` actually launches a real browser against (routes into filler.fill_application when applier.is_workday(url) and linkcheck passes) -- observation only, not a proposed change: this is the one place the safety net is weakest exactly where an unattended browser launch is the most consequential.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=3/3real browser=False -- jobagent/tailor.py:48
  SUMMARY: Single/double-letter unit suffix (mm/k/m/b) in _NUM_RE requires no whitespace before it, unlike the spelled-out word alternative which does allow a leading space, so space-separated finance shorthand is not scaled.
  SCENARIO: extract_facts("$5 mm budget") returns Fact(value=5.0, currency=True) instead of 5,000,000 (compare extract_facts("$5mm budget") which correctly gives 5,000,000). A tailored rewrite from "$5 mm budget" to the equivalent "$5,000,000 budget" is then wrongly reported by validate() as both a new number introduced (5,000,000) and a dropped number (5) even though the amount never changed. Verified live: tailor.validate('managed a $5 mm budget', 'managed a $5,000,000 budget').ok == False.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=3/3real browser=False -- jobagent/tailor.py:47
  SUMMARY: The % symbol alternative in _NUM_RE also lacks a leading \s*, so a percent sign separated from its number by a space is not recognized as a percentage at all and is captured as a plain 'value' fact instead of a 'pct' fact.
  SCENARIO: extract_facts("35 % region") returns Fact(kind='value', value=35.0) instead of Fact(kind='pct', value=35.0) (compare extract_facts("35% region") which correctly returns kind='pct'). validate('grew by 35%', 'grew by 35 %') reports '35%' as dropped and '35' as added even though only a space was inserted and the meaning is identical. Verified live.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=high votes=3/3real browser=False -- main.py:100
  SUMMARY: _scan_sources() calls fut.result() with no try/except, so an unhandled exception from any single board's fetch() (see the workday/lever/greenhouse findings above) kills the entire multi-board scan instead of being isolated to that one source.
  SCENARIO: Given any of the crash scenarios above happening in one configured Workday/Lever/Greenhouse board among several, `pool.submit(fn, *args)` captures the exception in the future; `found.extend(fut.result())` at line 100 re-raises it when iterated, uncaught, so _scan_sources() (and the whole `main.py scan` command) aborts even though the other boards fetched fine -- contradicting the sources' own '# noqa: BLE001 - source failures shouldn't crash a scan' comments, which only protect against exceptions raised directly inside get_json(), not the unguarded code paths documented in the other findings.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=3/3real browser=False -- dashboard.py:151
  SUMMARY: The '✓ mark as applied' button is rendered unconditionally on every job row and posts straight to /status/<lid>/applied, which (unlike main.py's cmd_render) has no forward-only guard, so a single click can silently regress a listing that's already at interview/offer/rejected back down to 'applied' with no confirmation.
  SCENARIO: A listing is already tracked as status='interview' (set earlier via `python main.py status <id> interview`). The dashboard still shows the same unconditional ✓ button for that row (dashboard.py:151, `<form method="post" action="/status/{{r['id']}}/applied">`). A misclick - very plausible since every row across dozens of listings has an identical-looking button in the same column - POSTs to /status/<lid>/applied. dashboard.py's set_status route (lines 472-481) only checks `st in database.STATUSES` (it is) and then calls database.set_status() unconditionally, which does a bare UPDATE with no comparison to the current status. The interview/offer tracking state is silently overwritten back to 'applied', with no undo and no visual warning, unlike main.py's own cmd_render which explicitly protects against exactly this kind of backward status move ('never move an already-advanced application... backward').
  AGENT_FIX_REPORT: None

[flagged-not-autofixed] sev=medium votes=3/2real browser=False -- dashboard.py:320
  SUMMARY: The /coach/<lid> route can block Flask's single-threaded dev server for minutes: matchcoach.suggest_all() makes one sequential, up-to-60s-timeout Ollama call per resume bullet, and dashboard.py's own app.run() call has no threaded=True.
  SCENARIO: matchcoach.suggest_all (jobagent/matchcoach.py:102-117) loops every experience/bullet and only stops early once `limit` (5) *successful* suggestions accumulate -- if most bullets honestly don't support any missing trait (the documented common case), it still calls rewrite_fn (a real network request, default timeout=60s) for every remaining bullet before returning. A resume with 20-30 bullets and an Ollama box that's alive but slow (large model still loading, CPU-only inference, etc. -- not fully down, which fails fast) can make a single /coach/<lid> request take many minutes. dashboard.py's own entrypoint calls `app.run(host="127.0.0.1", port=5000, debug=False)` (dashboard.py:485) with no `threaded=True`, so Werkzeug's dev server handles one request at a time -- during that multi-minute call, the whole dashboard (job list, Apply buttons, queue view, status buttons) is unresponsive with no progress indicator and no way to cancel; it just looks hung/crashed.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=3/3real browser=False -- jobagent/applyqueue.py:121
  SUMMARY: The mark() calls inside run()'s try/except (both the success path and the except-handler's error path) are not themselves guarded, so a DB write failure there is uncaught and kills the whole batch loop mid-run.
  SCENARIO: run()'s only exception handling is around apply_fn(lid) (applyqueue.py:116-121); the mark() call at line 119 (success) is inside the try, but if it (or the except-handler's mark() at line 121) raises - e.g. sqlite3.OperationalError('database is locked') from a concurrent writer, or an OS-level file lock/IO error from OneDrive actively syncing the jobs.db file mid-write (this repo lives in an OneDrive-synced folder per the workspace CLAUDE.md, and jobagent/database.py's connect() uses plain sqlite3.connect() with no WAL mode or custom busy timeout) - the exception propagates straight out of run(), terminating the while loop entirely. The job being processed at that moment is left permanently in 'running' (only fixable via reset_stuck/queue reset), and every other job still 'queued' behind it is silently abandoned mid-batch with no error surfaced beyond the crashed CLI process - a user who kicked off `queue run` and walked away (the documented headline workflow) would return to a queue that stopped partway with no explanation.
  AGENT_FIX_REPORT: None

[flagged-not-autofixed] sev=medium votes=3/3real browser=False -- jobagent/attempts.py:28
  SUMMARY: _key() only lowercases and collapses whitespace - it does no canonicalization, so the same real employer listed under slightly different company-name strings gets independent attempt-cap allowances.
  SCENARIO: Job sources (greenhouse/lever/workday feeds) frequently render a company's display name differently per posting or per source (e.g. 'Citi', 'Citigroup', 'Citi Inc.', 'Citi, N.A.'). Since _key(company) = ' '.join(company.lower().split()) does exact-match normalization only, allowed()/record() (attempts.py:45-50, 53-66) treat each spelling as a distinct company with its own fresh CAP=3 budget. A batch queue run (or repeated manual applies) against several listings for the same real employer under different name variants can fire well beyond 3 automated attempts at that employer, defeating the safety rail's stated purpose ('never auto-apply to one company more than attempts.CAP times').
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=3/3real browser=False -- jobagent/database.py:147
  SUMMARY: ranked_listings() silently drops any listing whose score is NULL, even at min_score=0.0, because SQL NULL comparisons are never true.
  SCENARIO: `ranked_listings` builds its filter as `WHERE l.score >= ?`. Verified directly against sqlite3: with one row score=NULL and one row score=10, `SELECT * FROM t WHERE score >= 0.0` returns only the row with 10 — NULL >= 0.0 evaluates to NULL, which the WHERE clause treats as false, not true. `upsert_listing` writes whatever `row.get('score')` returns (None if absent) with no default, and the `listings.score` column has no NOT NULL/DEFAULT constraint, so a listing inserted without a 'score' key gets a NULL row. Today the only production writer (`main.py cmd_scan`) always computes and passes a numeric score, so this is currently latent — but tests/test_dashboard_queue_report.py and tests/test_queue_link_validation.py already call `upsert_listing` without 'score' (proving how easy it is to hit), and any future insertion path (e.g. a manual 'add job by URL' feature, plausible per this project's stated direction toward a sellable product) would cause that job to silently vanish from the '/' dashboard, `main.py list`, and `main.py summarize`'s candidate pool with zero error or log — it just never appears, at any min_score threshold including 0.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=3/3real browser=False -- jobagent/database.py:92
  SUMMARY: prune_stale_listings' documented safety guarantee ('a transient fetch failure never wipes real jobs') can be silently defeated by a hostname-casing mismatch between how a Workday source's `source` value is stored vs. how it's recomputed for the 'attempted' set, causing real listings to be deleted on a mere network blip.
  SCENARIO: prune_stale_listings keeps a stale row only if `source in succeeded or source not in attempted` (database.py:118). Both sets are built by the caller, `main.py cmd_scan` (main.py:142-146): `succeeded={l.source for l in listings}` and `attempted=_configured_source_keys(cfg)` (main.py:104-115). For a Workday board, the listing's `source` is `f"workday:{tenant}"` where `tenant` is the regex-captured, ORIGINAL-CASE subdomain from the configured URL (jobagent/sources/workday.py:32,71 — case is preserved because `re.I` only makes matching case-insensitive, not the captured text). But `_configured_source_keys` derives its key via `urlparse(u).hostname.split('.')[0]` (main.py:112-114), and `urlparse(...).hostname` is *always lowercased* by Python's urllib — confirmed by direct test: for `https://Citi.wd5.myworkdayjobs.com/...`, the stored source is `workday:Citi` but `_configured_source_keys` produces `workday:citi`. Concrete scenario: config.json has a Workday URL with any uppercase letter in the tenant (e.g. pasted from a browser/marketing page as `Citi...`). On a scan where that board's fetch times out or returns 0 results (a one-off network blip — exactly the case this function's docstring says must be safe), `succeeded` won't contain `workday:Citi`, and the case-sensitive Python `in` check makes `'workday:Citi' not in {'workday:citi'}` True, so every one of that employer's stale-looking rows (anything not touched since scan_start) gets deleted even though the source is still fully configured — real, silent data loss the function was explicitly designed to prevent.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=3/3real browser=False -- jobagent/guardrail.py:28
  SUMMARY: _field_answer's closed _TRUE_TOKENS/_FALSE_TOKENS set has no punctuation stripping and no casual-synonym handling, so a legitimate, unambiguous stored answer-bank value can fail to parse and route an otherwise fully-answerable universal question to NEEDS_HUMAN.
  SCENARIO: Confirmed: guardrail.resolve('Are you at least 18 years of age?', {'is_over_18': 'No.'}) returns NEEDS_HUMAN (trailing period breaks the exact-token match), as do values like 'Nope' or 'Yep'. On its own this only adds unnecessary human review (fail-safe), but as shown in the screening.py finding above, this same parsing miss is exactly what pushes a known, populated field into the AI fallback branch where the missing relevance-check can substitute a wrong answer from an unrelated field.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=3/3real browser=False -- jobagent/matchcoach.py:91
  SUMMARY: suggest_rewrite's guard relies solely on tailor.validate(...).ok, but tailor.validate only warns (never sets ok=False) on spelled-out quantity-word changes -- so a fabricated change to a written-out number/duration is not 'dropped outright' as this module's own docstring (lines 5-10) promises; it is surfaced as a normal suggestion with the discrepancy buried in 'cautions'.
  SCENARIO: suggest_rewrite('Led a team of three analysts for two years.', 'jd text', ['leadership'], profile, rewrite_fn=lambda b,j,t: ('Led a team of five analysts for four years.', 'leadership')) returns a non-None suggestion dict (original vs suggested text with 'three'->'five' and 'two'->'four' fabricated) instead of None. The change is only visible if the human reads through the 'cautions' list buried in the returned dict, contradicting the stated guarantee that fact-altering rewrites are 'never shown to the human'. Verified live.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=3/3real browser=False -- jobagent/qbank.py:29
  SUMMARY: load() never validates that the parsed JSON is actually an object; a top-level list/string/number crashes dashboard.py's Answers page and qbank.answer()/pending().
  SCENARIO: Verified directly: if screening_answers.json ever contains valid-but-wrong-shaped JSON (e.g. '[]' -- plausible from a half-completed/interleaved write during the race in the finding above that happens to still parse, or a slip while hand-editing the file, which is a plain JSON file sitting right next to a UI meant to be edited by a non-coder), `json.loads()` returns a list, and dashboard.py's GET /answers handler does `items = sorted(data.items(), ...)` (line 364), which raises `AttributeError: 'list' object has no attribute 'items'`, crashing the page with a 500. The exact same shape mismatch would blow up qbank.answer()/pending() (both do `load().items()`), crashing mid-apply-run wherever they're called.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=2/2real browser=False -- jobagent/qbank.py:44
  SUMMARY: answer()/pending() assume every stored answer value is a string; a truthy non-string value (e.g. a number) crashes with AttributeError on .strip().
  SCENARIO: Verified directly: with data = {'How many years of experience?': 5} (a non-string, non-empty JSON value -- reachable via any hand-edit of the plain-JSON store, which is exactly the kind of file a non-coder user might poke at directly, or via any future writer that doesn't go through the current str()-only dashboard form path), both `answer()`'s `(a or '').strip()` and `pending()`'s identical expression raise `AttributeError: 'int' object has no attribute 'strip'`. Nothing in the current dashboard write path can create this today (form values are always Python str), so it's currently latent rather than actively triggered, but load() does nothing to guard against it and the file's whole purpose is to be a simple, human-editable JSON store.
  AGENT_FIX_REPORT: None

[flagged-not-autofixed] sev=medium votes=3/3real browser=False -- jobagent/scorer.py:169
  SUMMARY: Match Coach's set of 'missing traits' is silently capped to the first 5 missing skills in alphabetical order, a truncation that was built for a display string and has nothing to do with which skills are most worth coaching toward.
  SCENARIO: scorer.score_listing computes `missing = sorted(skills - skill_hits)[:5]` purely to keep the one-line 'missing: ...' reason string short for the job list UI. dashboard.py's coach() route re-parses that same already-truncated string via summarize.match_breakdown()['missing'] (dashboard.py:315) and passes it straight to matchcoach.suggest_all() as the entire population of traits Match Coach will ever try to address. A job missing 15 skills will only ever get rewrite suggestions aimed at whichever 5 sort first alphabetically -- e.g. 'sql' or 'vba' can never be offered a rewrite if 5 other missing skills (by pure alphabetical accident) precede them -- and the coach page gives no indication that other missing traits exist beyond the ones shown.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=3/3real browser=False -- jobagent/sources/greenhouse.py:24
  SUMMARY: data.get("jobs", []) only falls back to [] when the 'jobs' key is absent; if Greenhouse ever sends "jobs": null explicitly, the for-loop crashes with TypeError.
  SCENARIO: A malformed/degraded-state Greenhouse board response like {"jobs": null, "meta": {...}} (key present, explicitly null, e.g. from an API bug or a board being reindexed) makes data.get("jobs", []) return None rather than the default, so `for job in data.get("jobs", []):` raises TypeError: 'NoneType' object is not iterable. This is outside the try/except at lines 13-17 (which only guards get_json), so it's uncaught and crashes fetch() for that board.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=3/3real browser=False -- jobagent/sources/lever.py:21
  SUMMARY: _to_iso_date's except clause (TypeError, ValueError, OSError) does not include OverflowError, so an Infinity or excessively large createdAt value crashes fetch() uncaught.
  SCENARIO: Verified locally: json.loads('{"createdAt": Infinity}') successfully parses to {'createdAt': inf} because Python's json module accepts Infinity/-Infinity as an extension. int(float('inf')) then raises OverflowError: cannot convert float infinity to integer -- not one of the caught exception types. Also verified that a very large-but-finite createdAt (e.g. 1e300) causes datetime.fromtimestamp() itself to raise OverflowError: timestamp out of range for platform time_t. Either case propagates out of _to_iso_date (called unguarded at lever.py:50) and crashes lever.fetch().
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=3/3real browser=False -- jobagent/sources/workday.py:46
  SUMMARY: The Workday listing POST has no raise_for_status()/status check, so an HTTP error or bot-challenge response with a JSON-ish content-type is silently treated as 'no more jobs' instead of being logged as a failure.
  SCENARIO: Workday rate-limits (429) or returns a WAF/bot-detection response. If the response's content-type header doesn't contain 'json', `posts` becomes `{}.get("jobPostings") or []` = [] and the loop breaks on line 52-53 with zero diagnostic output beyond the final generic '0 metro matches' print — indistinguishable from a genuinely empty board. Unlike greenhouse/lever (which call raise_for_status() inside get_json() and log 'fetch failed: ...'), workday.py bypasses get_json entirely so this class of failure is never surfaced to the user.
  AGENT_FIX_REPORT: None

[flagged-not-autofixed] sev=medium votes=3/3real browser=False -- jobagent/tailor.py:44
  SUMMARY: The number pattern \d[\d,]*(?:\.\d+)? has no leading sign group, so a negative number's sign can be dropped or flipped between original and tailored text without being detected as a changed fact.
  SCENARIO: tailor.validate('grew headcount by -12', 'cut headcount by 12') returns ok=True with no errors and no warnings -- a metric that went from a decrease of 12 to an increase of 12 is treated as an unchanged fact because only the digits '12' are compared, not the sign. Verified live.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=medium votes=3/3real browser=False -- jobagent/tailor.py:210
  SUMMARY: The entity-lock's stop-word set only excludes three sentence-initial verbs (led/managed/built), but _proper_nouns() (lines 158-160) matches any capitalized token including ordinary verbs that happen to start a sentence -- so nearly every verbiage-only paraphrase that changes a bullet's opening verb triggers a 'new capitalized term / possible invented employer' warning.
  SCENARIO: Using the exact MASTER/tailored pair from tests/test_tailor.py::test_verbiage_only_edit_passes (only the opening verbs of each line change: Increased/Led/Cut -> Drove/Directed/Reduced), tailor.validate(MASTER, tailored) produces the warning "New capitalized terms appeared - confirm these aren't invented employers/tools/claims: Drove, Directed, Reduced" even though nothing was invented. Because this is exactly the kind of edit tailor.py's own AI brief asks for ('Edit VERBIAGE ONLY'), this warning fires on nearly every tailoring pass, risking alert fatigue that could cause a human reviewer to stop reading real invented-entity warnings. Verified live.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=low votes=3/3real browser=False -- app.py:104
  SUMMARY: main() discards _wait_up()'s return value, so a Flask startup failure (e.g. port 5000 already in use by another running instance) still opens the pywebview window pointed at a server that never came up, with no visible error in a --windowed frozen build.
  SCENARIO: `threading.Thread(target=_serve, daemon=True).start(); _wait_up(); webview.create_window(...)`. If _serve()'s `app.run(host=HOST, port=PORT, ...)` raises (e.g. OSError: port already bound, which is plausible if the user still has a previous instance of the app or `python dashboard.py` running), the exception just gets printed to stderr by the daemon thread and the main thread is not notified. _wait_up() then polls for the full 10s timeout, returns False, and that False is never checked -- the webview window opens anyway pointing at a dead http://127.0.0.1:5000, showing a browser-style connection error inside the app chrome. In the frozen --windowed build there is no console to show the real underlying OSError, so the user just sees a broken window with no clue why.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=low votes=3/3real browser=False -- dashboard.py:441
  SUMMARY: queue_run()'s double-launch guard reads apply_queue state before the spawned `main.py queue run` child process has had a chance to mark anything 'running', so a fast double-click (or a click during the child's ~1-2s Python/import startup) can launch two concurrent unattended queue grinders that race to claim the same queued listing_id.
  SCENARIO: Click 'Run queue' twice in quick succession (or once, then again while the freshly-launched `python main.py queue run` subprocess is still importing playwright/etc. before it reaches applyqueue.run's first `mark(conn, lid, 'running')`). Both invocations of dashboard.py's queue_run() see `already_running = False` (no apply_queue row is 'running' yet) and `has_queued = True`, so both call subprocess.Popen(_cli_command('queue','run')). The two independent child processes then both call applyqueue.next_queued(conn) against the same SQLite file; nothing prevents both from reading the same queued listing_id before either process's mark(conn, lid, 'running') commits (jobagent/applyqueue.py lines 111-117: SELECT then UPDATE are two separate uncoordinated statements, no transaction/lock spans them). Two Playwright browser instances can end up driving the Workday wizard for the same application concurrently.
  AGENT_FIX_REPORT: None

[documented-only] sev=low votes=2/2real browser=True -- jobagent/applier.py:149
  SUMMARY: OBSERVATION ONLY, no fix proposed (file is off-limits per project rules): the leftover-fields loop calls qbank.record_unknown() separately for each field instead of batching, so a form with many blank/required fields performs many sequential load-modify-save cycles against screening_answers.json over the course of one apply run.
  SCENARIO: Each iteration of the loop at applier.py lines 147-151 is its own full qbank.record_unknown() call (its own load(), membership check, and save()). This is purely a widening of the race window described in the qbank.py findings above -- a form that leaves N fields blank produces N separate rewrites of screening_answers.json per apply run, each one an opportunity to race a concurrent dashboard save(). Noting this only as context for how often the qbank.py race window opens in real usage; not proposing any change to applier.py itself.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=low votes=3/3real browser=False -- jobagent/applyqueue.py:119
  SUMMARY: run() silently coerces any apply_fn return value that isn't a recognized STATES string into 'filled' (a ready-to-review success state) rather than a conservative/error state.
  SCENARIO: `mark(conn, lid, state if state in STATES else "filled", detail)` (applyqueue.py:119) means any bug or future change in the calling apply_fn (main.py's apply_one, or a future caller) that returns an unrecognized string (a typo, a new status not yet added to STATES, or an accidental `None` from a code path that forgets to return) gets recorded as 'filled' - the same status used for a correctly completed application ready for human review at the Review page. This masks a caller-side bug as a false-positive success in the Queue UI, potentially causing a reviewer to give less scrutiny to a job that actually failed in some untracked way, rather than surfacing it as 'error'/'needs_human'.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=low votes=3/3real browser=False -- jobagent/applyqueue.py:83
  SUMMARY: next_queued()'s 'FIFO by insertion time' ordering relies on datetime('now') which only has 1-second resolution, so jobs enqueued (or re-enqueued) within the same second have no guaranteed relative processing order.
  SCENARIO: next_queued() orders by `updated_at` (applyqueue.py:82-84), populated via SQLite's `datetime('now')` which resolves to whole seconds. A batch `queue add id1 id2 id3 id4 id5` (main.py's cmd_queue 'add' loop, main.py:462-467) inserts all rows within milliseconds of each other - almost certainly the same second - so their `updated_at` values tie, and which one next_queued() actually returns first is an accident of SQLite's internal row ordering, not a documented FIFO guarantee. This is currently masked in tests (test_applyqueue.py's test_enqueue_next_mark_round_trip only enqueues two rows and happens to observe insertion order), but is not guaranteed behavior, and INSERT OR REPLACE (used by enqueue for a re-add) changes the row's underlying rowid, which can further perturb tie-break order versus what a user expects from 'the order I added them'.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=low votes=3/3real browser=False -- jobagent/config.py:67
  SUMMARY: load_config() silently no-ops on a mistyped top-level config.json section name instead of warning, so a user's intended override can have zero effect with no error.
  SCENARIO: `for section, values in user.items(): ... if isinstance(values, dict) and section in cfg: cfg[section].update(values) else: cfg[section] = values`. If Ethan edits config.json and typos a section name (e.g. 'scoreing' instead of 'scoring', or 'target' instead of 'targets'), `section in cfg` is False, so the whole mistyped block is just added as an inert extra top-level key -- the real 'scoring'/'targets' section keeps 100% of DEFAULT_CONFIG's values. There's no schema validation anywhere (main.py's `doctor` command only checks imports/env, not config shape), so the change silently does nothing and nothing tells the user why their salary_floor or scoring-weight edit didn't take effect.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=low votes=3/3real browser=False -- jobagent/database.py:126
  SUMMARY: set_status() performs an UPDATE with no existence check or affected-row feedback, so a status change on an already-deleted listing_id silently no-ops while the caller (dashboard.py) reports success.
  SCENARIO: database.set_status runs `UPDATE applications SET status=?, ... WHERE listing_id=?` and returns nothing; it never checks `cursor.rowcount`. dashboard.py's `/status/<lid>/<st>` route (dashboard.py:472-481) calls it and unconditionally redirects to '/' as if it worked, with no existence check beforehand. Concrete scenario: user loads the dashboard ('/'), a background `python main.py scan` runs and prunes a 'found'-status listing that's gone stale (prune_stale_listings deletes both the listings row and its applications row, database.py:120-122), then the user — still looking at the old page — clicks 'Mark as Applied' on that now-deleted listing. The UPDATE matches zero rows, no exception is raised, the page redirects normally, and the user has no indication the click did nothing; the intended status change is silently lost.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=low votes=3/3real browser=False -- jobagent/sources/greenhouse.py:25
  SUMMARY: (job.get("location") or {}).get("name") assumes location is always a dict or falsy; a non-empty non-dict value (spec violation) would raise AttributeError.
  SCENARIO: If a Greenhouse job entry ever sent "location": "NYC" (a bare string rather than the expected {"name": ...} object -- an unexpected-shape response, low real-world likelihood but not ruled out for a third-party API), "NYC" is truthy so `or {}` does not kick in, and `.get("name")` on a str raises AttributeError, uncaught, crashing fetch(). Lower confidence than the other findings since Greenhouse's documented schema always nests location as an object.
  AGENT_FIX_REPORT: None

[fix-attempted] sev=low votes=3/3real browser=False -- main.py:177
  SUMMARY: `--limit` for `python main.py list` is parsed as a plain int with no lower-bound check, so a negative value (which argparse happily accepts as a valid int, e.g. `--limit -5`) turns `rows[: args.limit]` into a negative-index slice that silently drops the last N rows instead of erroring on a nonsensical limit.
  SCENARIO: Running `python main.py list --limit -5` parses cleanly to args.limit = -5 (verified: argparse's negative-number heuristic accepts it since no defined option looks like a negative number), then `rows[: -5]` returns all but the last 5 ranked listings - a confusing, silently-wrong result (looks like 'show me negative five' produced *some* jobs, not an error) rather than a clear 'limit must be positive' message.
  AGENT_FIX_REPORT: None
